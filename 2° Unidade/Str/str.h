/* ira ver o tamanho da string, ate o final dela */
int comprimento (char* s);

/* esta copiando o origem em destino */
void copia (char* dest, char* orig);

/* junta as duas string em uma unica, mas aqui ela so esta falando o total de caracteres */
void concatena (char* dest, char* orig);